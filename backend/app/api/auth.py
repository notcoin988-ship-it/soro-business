"""Вход в консоль (раздел 9 ТЗ).

ОТВЕТСТВЕННОСТЬ: один общий логин-пароль на воркспейс и подписанная
сессионная cookie (`itsdangerous`).

ГРАНИЦЫ: регистрации, ролей и восстановления пароля НЕТ — раздел 1.2
выносит это за скобки версии. Пользователей тоже нет как сущности:
`CONSOLE_LOGIN` и `CONSOLE_PASSWORD` берутся из `.env`.

ОТДЕЛЬНЫМ ПУНКТОМ ЧЕК-ЛИСТА: в `.env.example` пароль — плейсхолдер
`поменять-обязательно`. Такие вещи доживают до прода чаще, чем кажется.

ЗАВИСИМОСТИ: config.
СТАТУС: не реализовано.
"""
