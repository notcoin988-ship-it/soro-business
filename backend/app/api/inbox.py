"""Инбокс оператора и WebSocket (раздел 9, экран 06).

ОТВЕТСТВЕННОСТЬ: список эскалированных диалогов, переписка целиком,
ответ оператора в канал клиента и живые уведомления.

СОСТАВ:
  GET  /api/inbox                     очередь диалогов;
  GET  /api/inbox/{conversation_id}   история по всем каналам сразу;
  POST /api/inbox/{id}/take           взять в работу (`taken_by`, `taken_at`);
  POST /api/inbox/{id}/reply          ответ уходит в тот канал, откуда
                                      пришёл клиент;
  POST /api/inbox/{id}/return         вернуть боту (`status='bot'`);
  WS   /ws/inbox                      новые эскалации и сообщения.

ГЛАВНОЕ В ЭКРАНЕ: оператор видит ОДИН диалог с сообщениями из разных
каналов — это и есть омниканальность в данных, ради неё заведён
`conversation`, общий для всех `channel_identities` контакта.

ЗАВИСИМОСТИ: models, channels.* (отправка ответа), core.escalation.
СТАТУС: не реализовано.
"""
