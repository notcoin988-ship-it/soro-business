"""Ядро: обработка входящего сообщения (раздел 4 ТЗ, «путь одного сообщения»).

ОТВЕТСТВЕННОСТЬ: единственная точка, через которую проходит сообщение из
любого канала. Каналы не думают — они только принимают и отдают.

ПУТЬ СООБЩЕНИЯ (порядок обязателен):

 1. найти или создать `contact` и `channel_identity` по каналу;
 2. найти или открыть `conversation` — общий для всех каналов контакта;
 3. `core.pii.mask` → в `messages.text` оригинал, в `text_masked` маска;
 4. если диалог в статусе `operator` — молчим, сообщение просто ложится
    в инбокс: бот не перебивает живого человека;
 5. проверить триггер оператора (`OPERATOR_REQUEST_RE`) → эскалация;
 6. `core.rag` — поиск фрагментов; ниже порога → эскалация `no_answer`;
 7. `core.llm` — ответ стримом, `chunks_used` и латентность в `messages`;
 8. запись в `audit_log` события `llm_call`.

ВХОД:  канал, внешний id отправителя, текст.
ВЫХОД: поток ответа + сохранённые сообщения.

ЗАВИСИМОСТИ: pii, rag, llm, escalation, models.
СТАТУС: не реализовано.
"""
