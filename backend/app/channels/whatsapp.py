"""WhatsApp через Meta Cloud API (раздел 7.3 ТЗ) — песочница.

ОТВЕТСТВЕННОСТЬ: webhook Meta: верификация подписки, приём сообщений,
отправка ответов. Как и Telegram — только транспорт.

ИДЕНТИФИКАЦИЯ: `external_id` = номер телефона. Отсюда же склейка контактов
между каналами — но она РУЧНАЯ: оператор нажимает «Объединить контакты»,
автоматики по номеру нет (раздел 5.1).

ГРАБЛИ: временный токен песочницы живёт 24 часа. Постоянный делается через
System User — отдельная задача, до демо её надо закрыть, иначе канал
отвалится в самый неудобный момент.

ЗАВИСИМОСТИ: core.dialog, models, `WHATSAPP_TOKEN`, `WHATSAPP_PHONE_ID`,
`WHATSAPP_VERIFY_TOKEN`.
СТАТУС: не реализовано.
"""
