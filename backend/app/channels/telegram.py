"""Telegram-бот (раздел 7.1 ТЗ) — делается первым из каналов.

ОТВЕТСТВЕННОСТЬ: принять webhook от Telegram, отдать текст в `core.dialog`,
вернуть ответ пользователю. Логики бота здесь нет — только транспорт.

РЕЖИМ: aiogram 3.7, **webhook**, не polling. Адрес — `PUBLIC_BASE_URL`
через ngrok, секрет — `TELEGRAM_WEBHOOK_SECRET` (проверять на каждом
запросе, иначе webhook открыт всему интернету).

ОСОБЕННОСТИ КАНАЛА:
* `/start <link_token>` — склейка с виджетом (раздел 5.1): токен ищется в
  Redis, найденной widget-identity переписывается `contact_id`;
* источники в ответе идут строкой-футером: «Манбаъ: Тарифҳо 2026, саҳ. 4»,
  а не кликабельными бейджами, как в виджете;
* `external_id` = telegram user id.

ГРАБЛИ: ngrok при перезапуске меняет адрес, и webhook молча замолкает.
Проверять `getWebhookInfo` перед каждым демо.

ЗАВИСИМОСТИ: core.dialog, models, Redis.
СТАТУС: не реализовано.
"""
